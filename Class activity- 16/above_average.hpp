#ifndef ABOVE_AVERAGE_HPP
#define ABOVE_AVERAGE_HPP

#include <vector>
#include <string>
void printAboveAverage(const std::vector<std::string>& names, const std::vector<int>& grades, double average);

#endif
